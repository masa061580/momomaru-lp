{
    "name": "portfolio-lp",
    "version": "1.0.0",
    "scripts": {
        "build": "echo 'No build step required'",
        "start": "serve . -p $PORT"
    },
    "dependencies": {
        "serve": "^14.2.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}